<form method="post" action="{{ path('info_comp_delete', {'id': info_comp.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ info_comp.id) }}">
    <button class="btn">Delete</button>
</form>
