<form method="post" action="{{ path('black_list_delete', {'id': black_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ black_list.id) }}">
    <button class="btn">Delete</button>
</form>
